# Copie este arquivo para .env.local e preencha com seus valores locais.
# .env.local é ignorado pelo git e sobrescreve as variáveis do .env.
# Nunca commite .env.local no controle de versão.

APP_ENV=dev
APP_SECRET=coloque_aqui_uma_string_hex_de_32_chars

DATABASE_URL="postgresql://beakon:beakon@postgres:5432/beakon?serverVersion=16&charset=utf8"

POSTGRES_DB=beakon
POSTGRES_USER=beakon
POSTGRES_PASSWORD=beakon
